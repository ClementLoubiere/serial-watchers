{% extends 'base.html.twig' %}

{% block body %}

        <img src="" alt="">

    <div class="container">
        <div class="row">

            
    {% for json in json_data %}
        <div class="col-3 text-center">
            {% for test in image %}
            <img src="{{ test }}" alt="">
            {% endfor %}
            <br>
            {{ json }}</div>
        
    {% endfor %}
        </div>
    </div>

{% endblock %}