{% extends 'base.html.twig' %}


{% block body %}

    {{ form_start(form) }}
    {{ form_row(form) }}
    <div class="text-center">
        <button type="submit" class="btn btn-primary col-5">
            Enregistrer
        </button>
    </div>
    {{ form_end(form) }}

{% endblock %}
