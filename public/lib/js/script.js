/*
$(document).ready(function(){

    $('.btn-addSerie').click(function (e) {
        e.preventDefault();
        $.ajax( "series?id=" + $(this).data("id"))
            .done(function() {
                alert( "success");
            })
            .fail(function() {
                alert( "error" );
            });
    });
}); */
