$(document).ready(function () {
    /*
    let key = 'f9966f8cc78884142eed6c6d4710717a';

    $.ajax({
        url: 'https://api.themoviedb.org/3/tv/popular?api_key=' + key + '&page=' + page, //General popular movies
        //url: url + mode + input + apiKey,
        jsonType: 'jsonp',
        jsonCallback: 'testing'
    })
        .error(function (e) {
            console.log(e.message);
        })
        .done(function (data) {
            console.log(data);
            for (let p = 0; p < data.page.length; p++) {
                $('.pagination').append('<li><a href="https://api.themoviedb.org/3/tv/popular?api_key=' + key + '&page=' + page + '">' + page + '</a></li>');
            }
        });*/
});